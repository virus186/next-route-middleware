import { useRouter } from "next/router";
import React from "react";
import Layout from "../../../components/Layout";

const id = (id) => {

  return (
    <Layout>
      <div>
        <h1>GeeksforGeeks</h1>
        
      </div>
    </Layout>
  );
};

export default id;
